(ns com.amazonaws.services.servicequotas.AWSServiceQuotasAsyncClientBuilder
  "Fluent builder for AWSServiceQuotasAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.servicequotas AWSServiceQuotasAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.servicequotas.AWSServiceQuotasAsyncClientBuilder`"
  (^com.amazonaws.services.servicequotas.AWSServiceQuotasAsyncClientBuilder []
    (AWSServiceQuotasAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.servicequotas.AWSServiceQuotasAsync`"
  (^com.amazonaws.services.servicequotas.AWSServiceQuotasAsync []
    (AWSServiceQuotasAsyncClientBuilder/defaultClient )))

