(ns com.amazonaws.services.inspector.AmazonInspectorAsyncClientBuilder
  "Fluent builder for AmazonInspectorAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.inspector AmazonInspectorAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.inspector.AmazonInspectorAsyncClientBuilder`"
  (^com.amazonaws.services.inspector.AmazonInspectorAsyncClientBuilder []
    (AmazonInspectorAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.inspector.AmazonInspectorAsync`"
  (^com.amazonaws.services.inspector.AmazonInspectorAsync []
    (AmazonInspectorAsyncClientBuilder/defaultClient )))

