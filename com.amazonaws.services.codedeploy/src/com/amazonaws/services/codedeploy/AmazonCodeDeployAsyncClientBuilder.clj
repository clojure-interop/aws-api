(ns com.amazonaws.services.codedeploy.AmazonCodeDeployAsyncClientBuilder
  "Fluent builder for AmazonCodeDeployAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.codedeploy AmazonCodeDeployAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.codedeploy.AmazonCodeDeployAsyncClientBuilder`"
  (^com.amazonaws.services.codedeploy.AmazonCodeDeployAsyncClientBuilder []
    (AmazonCodeDeployAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.codedeploy.AmazonCodeDeployAsync`"
  (^com.amazonaws.services.codedeploy.AmazonCodeDeployAsync []
    (AmazonCodeDeployAsyncClientBuilder/defaultClient )))

