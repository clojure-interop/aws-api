(ns com.amazonaws.services.servicecatalog.AWSServiceCatalogAsyncClientBuilder
  "Fluent builder for AWSServiceCatalogAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.servicecatalog AWSServiceCatalogAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.servicecatalog.AWSServiceCatalogAsyncClientBuilder`"
  (^com.amazonaws.services.servicecatalog.AWSServiceCatalogAsyncClientBuilder []
    (AWSServiceCatalogAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.servicecatalog.AWSServiceCatalogAsync`"
  (^com.amazonaws.services.servicecatalog.AWSServiceCatalogAsync []
    (AWSServiceCatalogAsyncClientBuilder/defaultClient )))

