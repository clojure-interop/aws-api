(ns com.amazonaws.services.servicecatalog.AWSServiceCatalogClientBuilder
  "Fluent builder for AWSServiceCatalog. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.servicecatalog AWSServiceCatalogClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.servicecatalog.AWSServiceCatalogClientBuilder`"
  (^com.amazonaws.services.servicecatalog.AWSServiceCatalogClientBuilder []
    (AWSServiceCatalogClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.servicecatalog.AWSServiceCatalog`"
  (^com.amazonaws.services.servicecatalog.AWSServiceCatalog []
    (AWSServiceCatalogClientBuilder/defaultClient )))

