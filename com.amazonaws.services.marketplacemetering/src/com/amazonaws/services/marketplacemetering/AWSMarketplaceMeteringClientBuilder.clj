(ns com.amazonaws.services.marketplacemetering.AWSMarketplaceMeteringClientBuilder
  "Fluent builder for AWSMarketplaceMetering. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.marketplacemetering AWSMarketplaceMeteringClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.marketplacemetering.AWSMarketplaceMeteringClientBuilder`"
  (^com.amazonaws.services.marketplacemetering.AWSMarketplaceMeteringClientBuilder []
    (AWSMarketplaceMeteringClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.marketplacemetering.AWSMarketplaceMetering`"
  (^com.amazonaws.services.marketplacemetering.AWSMarketplaceMetering []
    (AWSMarketplaceMeteringClientBuilder/defaultClient )))

