(ns com.amazonaws.services.simpleemail.AmazonSimpleEmailServiceAsyncClientBuilder
  "Fluent builder for AmazonSimpleEmailServiceAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.simpleemail AmazonSimpleEmailServiceAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.simpleemail.AmazonSimpleEmailServiceAsyncClientBuilder`"
  (^com.amazonaws.services.simpleemail.AmazonSimpleEmailServiceAsyncClientBuilder []
    (AmazonSimpleEmailServiceAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.simpleemail.AmazonSimpleEmailServiceAsync`"
  (^com.amazonaws.services.simpleemail.AmazonSimpleEmailServiceAsync []
    (AmazonSimpleEmailServiceAsyncClientBuilder/defaultClient )))

