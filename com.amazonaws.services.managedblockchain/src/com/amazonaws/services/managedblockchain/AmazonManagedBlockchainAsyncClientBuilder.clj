(ns com.amazonaws.services.managedblockchain.AmazonManagedBlockchainAsyncClientBuilder
  "Fluent builder for AmazonManagedBlockchainAsync. Use of the builder
  is preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.managedblockchain AmazonManagedBlockchainAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.managedblockchain.AmazonManagedBlockchainAsyncClientBuilder`"
  (^com.amazonaws.services.managedblockchain.AmazonManagedBlockchainAsyncClientBuilder []
    (AmazonManagedBlockchainAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.managedblockchain.AmazonManagedBlockchainAsync`"
  (^com.amazonaws.services.managedblockchain.AmazonManagedBlockchainAsync []
    (AmazonManagedBlockchainAsyncClientBuilder/defaultClient )))

