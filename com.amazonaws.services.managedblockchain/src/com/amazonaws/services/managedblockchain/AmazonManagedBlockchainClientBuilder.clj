(ns com.amazonaws.services.managedblockchain.AmazonManagedBlockchainClientBuilder
  "Fluent builder for AmazonManagedBlockchain. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.managedblockchain AmazonManagedBlockchainClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.managedblockchain.AmazonManagedBlockchainClientBuilder`"
  (^com.amazonaws.services.managedblockchain.AmazonManagedBlockchainClientBuilder []
    (AmazonManagedBlockchainClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.managedblockchain.AmazonManagedBlockchain`"
  (^com.amazonaws.services.managedblockchain.AmazonManagedBlockchain []
    (AmazonManagedBlockchainClientBuilder/defaultClient )))

