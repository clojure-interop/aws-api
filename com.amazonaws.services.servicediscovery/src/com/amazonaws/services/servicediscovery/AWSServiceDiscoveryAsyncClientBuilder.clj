(ns com.amazonaws.services.servicediscovery.AWSServiceDiscoveryAsyncClientBuilder
  "Fluent builder for AWSServiceDiscoveryAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.servicediscovery AWSServiceDiscoveryAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.servicediscovery.AWSServiceDiscoveryAsyncClientBuilder`"
  (^com.amazonaws.services.servicediscovery.AWSServiceDiscoveryAsyncClientBuilder []
    (AWSServiceDiscoveryAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.servicediscovery.AWSServiceDiscoveryAsync`"
  (^com.amazonaws.services.servicediscovery.AWSServiceDiscoveryAsync []
    (AWSServiceDiscoveryAsyncClientBuilder/defaultClient )))

