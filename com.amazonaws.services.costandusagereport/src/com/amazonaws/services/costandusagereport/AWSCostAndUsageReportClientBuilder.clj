(ns com.amazonaws.services.costandusagereport.AWSCostAndUsageReportClientBuilder
  "Fluent builder for AWSCostAndUsageReport. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.costandusagereport AWSCostAndUsageReportClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.costandusagereport.AWSCostAndUsageReportClientBuilder`"
  (^com.amazonaws.services.costandusagereport.AWSCostAndUsageReportClientBuilder []
    (AWSCostAndUsageReportClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.costandusagereport.AWSCostAndUsageReport`"
  (^com.amazonaws.services.costandusagereport.AWSCostAndUsageReport []
    (AWSCostAndUsageReportClientBuilder/defaultClient )))

