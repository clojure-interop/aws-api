(ns com.amazonaws.services.costandusagereport.AWSCostAndUsageReportAsyncClientBuilder
  "Fluent builder for AWSCostAndUsageReportAsync. Use of the builder
  is preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.costandusagereport AWSCostAndUsageReportAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.costandusagereport.AWSCostAndUsageReportAsyncClientBuilder`"
  (^com.amazonaws.services.costandusagereport.AWSCostAndUsageReportAsyncClientBuilder []
    (AWSCostAndUsageReportAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.costandusagereport.AWSCostAndUsageReportAsync`"
  (^com.amazonaws.services.costandusagereport.AWSCostAndUsageReportAsync []
    (AWSCostAndUsageReportAsyncClientBuilder/defaultClient )))

