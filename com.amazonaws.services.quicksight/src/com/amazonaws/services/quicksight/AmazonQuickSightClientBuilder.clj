(ns com.amazonaws.services.quicksight.AmazonQuickSightClientBuilder
  "Fluent builder for AmazonQuickSight. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.quicksight AmazonQuickSightClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.quicksight.AmazonQuickSightClientBuilder`"
  (^com.amazonaws.services.quicksight.AmazonQuickSightClientBuilder []
    (AmazonQuickSightClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.quicksight.AmazonQuickSight`"
  (^com.amazonaws.services.quicksight.AmazonQuickSight []
    (AmazonQuickSightClientBuilder/defaultClient )))

