(ns com.amazonaws.services.opsworks.AWSOpsWorksAsyncClientBuilder
  "Fluent builder for AWSOpsWorksAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.opsworks AWSOpsWorksAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.opsworks.AWSOpsWorksAsyncClientBuilder`"
  (^com.amazonaws.services.opsworks.AWSOpsWorksAsyncClientBuilder []
    (AWSOpsWorksAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.opsworks.AWSOpsWorksAsync`"
  (^com.amazonaws.services.opsworks.AWSOpsWorksAsync []
    (AWSOpsWorksAsyncClientBuilder/defaultClient )))

