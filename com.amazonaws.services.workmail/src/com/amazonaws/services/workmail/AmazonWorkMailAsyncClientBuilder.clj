(ns com.amazonaws.services.workmail.AmazonWorkMailAsyncClientBuilder
  "Fluent builder for AmazonWorkMailAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.workmail AmazonWorkMailAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.workmail.AmazonWorkMailAsyncClientBuilder`"
  (^com.amazonaws.services.workmail.AmazonWorkMailAsyncClientBuilder []
    (AmazonWorkMailAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.workmail.AmazonWorkMailAsync`"
  (^com.amazonaws.services.workmail.AmazonWorkMailAsync []
    (AmazonWorkMailAsyncClientBuilder/defaultClient )))

