(ns com.amazonaws.services.glue.AWSGlueAsyncClientBuilder
  "Fluent builder for AWSGlueAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.glue AWSGlueAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.glue.AWSGlueAsyncClientBuilder`"
  (^com.amazonaws.services.glue.AWSGlueAsyncClientBuilder []
    (AWSGlueAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.glue.AWSGlueAsync`"
  (^com.amazonaws.services.glue.AWSGlueAsync []
    (AWSGlueAsyncClientBuilder/defaultClient )))

