(ns com.amazonaws.services.mediapackagevod.AWSMediaPackageVodClientBuilder
  "Fluent builder for AWSMediaPackageVod. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.mediapackagevod AWSMediaPackageVodClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.mediapackagevod.AWSMediaPackageVodClientBuilder`"
  (^com.amazonaws.services.mediapackagevod.AWSMediaPackageVodClientBuilder []
    (AWSMediaPackageVodClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.mediapackagevod.AWSMediaPackageVod`"
  (^com.amazonaws.services.mediapackagevod.AWSMediaPackageVod []
    (AWSMediaPackageVodClientBuilder/defaultClient )))

