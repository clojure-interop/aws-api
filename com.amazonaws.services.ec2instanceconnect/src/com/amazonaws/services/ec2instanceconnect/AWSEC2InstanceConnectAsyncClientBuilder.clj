(ns com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectAsyncClientBuilder
  "Fluent builder for AWSEC2InstanceConnectAsync. Use of the builder
  is preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.ec2instanceconnect AWSEC2InstanceConnectAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectAsyncClientBuilder`"
  (^com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectAsyncClientBuilder []
    (AWSEC2InstanceConnectAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectAsync`"
  (^com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectAsync []
    (AWSEC2InstanceConnectAsyncClientBuilder/defaultClient )))

