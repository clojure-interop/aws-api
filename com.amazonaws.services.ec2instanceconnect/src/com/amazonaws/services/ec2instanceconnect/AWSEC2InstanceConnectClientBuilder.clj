(ns com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectClientBuilder
  "Fluent builder for AWSEC2InstanceConnect. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.ec2instanceconnect AWSEC2InstanceConnectClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectClientBuilder`"
  (^com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnectClientBuilder []
    (AWSEC2InstanceConnectClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnect`"
  (^com.amazonaws.services.ec2instanceconnect.AWSEC2InstanceConnect []
    (AWSEC2InstanceConnectClientBuilder/defaultClient )))

