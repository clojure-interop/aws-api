(ns com.amazonaws.services.appstream.AmazonAppStreamAsyncClientBuilder
  "Fluent builder for AmazonAppStreamAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.appstream AmazonAppStreamAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.appstream.AmazonAppStreamAsyncClientBuilder`"
  (^com.amazonaws.services.appstream.AmazonAppStreamAsyncClientBuilder []
    (AmazonAppStreamAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.appstream.AmazonAppStreamAsync`"
  (^com.amazonaws.services.appstream.AmazonAppStreamAsync []
    (AmazonAppStreamAsyncClientBuilder/defaultClient )))

