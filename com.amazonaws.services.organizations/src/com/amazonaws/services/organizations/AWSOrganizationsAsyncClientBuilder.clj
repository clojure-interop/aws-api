(ns com.amazonaws.services.organizations.AWSOrganizationsAsyncClientBuilder
  "Fluent builder for AWSOrganizationsAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.organizations AWSOrganizationsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.organizations.AWSOrganizationsAsyncClientBuilder`"
  (^com.amazonaws.services.organizations.AWSOrganizationsAsyncClientBuilder []
    (AWSOrganizationsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.organizations.AWSOrganizationsAsync`"
  (^com.amazonaws.services.organizations.AWSOrganizationsAsync []
    (AWSOrganizationsAsyncClientBuilder/defaultClient )))

