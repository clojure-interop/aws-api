(ns com.amazonaws.services.alexaforbusiness.AmazonAlexaForBusinessClientBuilder
  "Fluent builder for AmazonAlexaForBusiness. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.alexaforbusiness AmazonAlexaForBusinessClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.alexaforbusiness.AmazonAlexaForBusinessClientBuilder`"
  (^com.amazonaws.services.alexaforbusiness.AmazonAlexaForBusinessClientBuilder []
    (AmazonAlexaForBusinessClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.alexaforbusiness.AmazonAlexaForBusiness`"
  (^com.amazonaws.services.alexaforbusiness.AmazonAlexaForBusiness []
    (AmazonAlexaForBusinessClientBuilder/defaultClient )))

