(ns com.amazonaws.services.robomaker.AWSRoboMakerAsyncClientBuilder
  "Fluent builder for AWSRoboMakerAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.robomaker AWSRoboMakerAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.robomaker.AWSRoboMakerAsyncClientBuilder`"
  (^com.amazonaws.services.robomaker.AWSRoboMakerAsyncClientBuilder []
    (AWSRoboMakerAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.robomaker.AWSRoboMakerAsync`"
  (^com.amazonaws.services.robomaker.AWSRoboMakerAsync []
    (AWSRoboMakerAsyncClientBuilder/defaultClient )))

