(ns com.amazonaws.services.storagegateway.AWSStorageGatewayAsyncClientBuilder
  "Fluent builder for AWSStorageGatewayAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.storagegateway AWSStorageGatewayAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.storagegateway.AWSStorageGatewayAsyncClientBuilder`"
  (^com.amazonaws.services.storagegateway.AWSStorageGatewayAsyncClientBuilder []
    (AWSStorageGatewayAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.storagegateway.AWSStorageGatewayAsync`"
  (^com.amazonaws.services.storagegateway.AWSStorageGatewayAsync []
    (AWSStorageGatewayAsyncClientBuilder/defaultClient )))

