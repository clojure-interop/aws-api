(ns com.amazonaws.services.iotanalytics.AWSIoTAnalyticsAsyncClientBuilder
  "Fluent builder for AWSIoTAnalyticsAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.iotanalytics AWSIoTAnalyticsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.iotanalytics.AWSIoTAnalyticsAsyncClientBuilder`"
  (^com.amazonaws.services.iotanalytics.AWSIoTAnalyticsAsyncClientBuilder []
    (AWSIoTAnalyticsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.iotanalytics.AWSIoTAnalyticsAsync`"
  (^com.amazonaws.services.iotanalytics.AWSIoTAnalyticsAsync []
    (AWSIoTAnalyticsAsyncClientBuilder/defaultClient )))

