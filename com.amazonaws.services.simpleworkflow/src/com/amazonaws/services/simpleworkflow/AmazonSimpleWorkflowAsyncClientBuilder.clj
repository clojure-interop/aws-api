(ns com.amazonaws.services.simpleworkflow.AmazonSimpleWorkflowAsyncClientBuilder
  "Fluent builder for AmazonSimpleWorkflowAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.simpleworkflow AmazonSimpleWorkflowAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.simpleworkflow.AmazonSimpleWorkflowAsyncClientBuilder`"
  (^com.amazonaws.services.simpleworkflow.AmazonSimpleWorkflowAsyncClientBuilder []
    (AmazonSimpleWorkflowAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.simpleworkflow.AmazonSimpleWorkflowAsync`"
  (^com.amazonaws.services.simpleworkflow.AmazonSimpleWorkflowAsync []
    (AmazonSimpleWorkflowAsyncClientBuilder/defaultClient )))

