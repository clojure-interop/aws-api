(ns com.amazonaws.services.dynamodbv2.AmazonDynamoDBStreamsAsyncClientBuilder
  "Fluent builder for AmazonDynamoDBStreamsAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.dynamodbv2 AmazonDynamoDBStreamsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.dynamodbv2.AmazonDynamoDBStreamsAsyncClientBuilder`"
  (^com.amazonaws.services.dynamodbv2.AmazonDynamoDBStreamsAsyncClientBuilder []
    (AmazonDynamoDBStreamsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.dynamodbv2.AmazonDynamoDBStreamsAsync`"
  (^com.amazonaws.services.dynamodbv2.AmazonDynamoDBStreamsAsync []
    (AmazonDynamoDBStreamsAsyncClientBuilder/defaultClient )))

