(ns com.amazonaws.services.acmpca.AWSACMPCAAsyncClientBuilder
  "Fluent builder for AWSACMPCAAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.acmpca AWSACMPCAAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.acmpca.AWSACMPCAAsyncClientBuilder`"
  (^com.amazonaws.services.acmpca.AWSACMPCAAsyncClientBuilder []
    (AWSACMPCAAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.acmpca.AWSACMPCAAsync`"
  (^com.amazonaws.services.acmpca.AWSACMPCAAsync []
    (AWSACMPCAAsyncClientBuilder/defaultClient )))

