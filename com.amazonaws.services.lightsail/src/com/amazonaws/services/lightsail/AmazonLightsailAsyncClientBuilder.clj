(ns com.amazonaws.services.lightsail.AmazonLightsailAsyncClientBuilder
  "Fluent builder for AmazonLightsailAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.lightsail AmazonLightsailAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.lightsail.AmazonLightsailAsyncClientBuilder`"
  (^com.amazonaws.services.lightsail.AmazonLightsailAsyncClientBuilder []
    (AmazonLightsailAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.lightsail.AmazonLightsailAsync`"
  (^com.amazonaws.services.lightsail.AmazonLightsailAsync []
    (AmazonLightsailAsyncClientBuilder/defaultClient )))

