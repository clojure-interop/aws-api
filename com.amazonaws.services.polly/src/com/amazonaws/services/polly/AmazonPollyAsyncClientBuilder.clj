(ns com.amazonaws.services.polly.AmazonPollyAsyncClientBuilder
  "Fluent builder for AmazonPollyAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.polly AmazonPollyAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.polly.AmazonPollyAsyncClientBuilder`"
  (^com.amazonaws.services.polly.AmazonPollyAsyncClientBuilder []
    (AmazonPollyAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.polly.AmazonPollyAsync`"
  (^com.amazonaws.services.polly.AmazonPollyAsync []
    (AmazonPollyAsyncClientBuilder/defaultClient )))

