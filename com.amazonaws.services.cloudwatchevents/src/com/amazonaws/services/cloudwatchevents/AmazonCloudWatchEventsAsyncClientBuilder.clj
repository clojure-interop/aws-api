(ns com.amazonaws.services.cloudwatchevents.AmazonCloudWatchEventsAsyncClientBuilder
  "Fluent builder for AmazonCloudWatchEventsAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cloudwatchevents AmazonCloudWatchEventsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cloudwatchevents.AmazonCloudWatchEventsAsyncClientBuilder`"
  (^com.amazonaws.services.cloudwatchevents.AmazonCloudWatchEventsAsyncClientBuilder []
    (AmazonCloudWatchEventsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cloudwatchevents.AmazonCloudWatchEventsAsync`"
  (^com.amazonaws.services.cloudwatchevents.AmazonCloudWatchEventsAsync []
    (AmazonCloudWatchEventsAsyncClientBuilder/defaultClient )))

