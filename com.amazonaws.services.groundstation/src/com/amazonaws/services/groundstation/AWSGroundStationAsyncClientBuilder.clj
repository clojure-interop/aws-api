(ns com.amazonaws.services.groundstation.AWSGroundStationAsyncClientBuilder
  "Fluent builder for AWSGroundStationAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.groundstation AWSGroundStationAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.groundstation.AWSGroundStationAsyncClientBuilder`"
  (^com.amazonaws.services.groundstation.AWSGroundStationAsyncClientBuilder []
    (AWSGroundStationAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.groundstation.AWSGroundStationAsync`"
  (^com.amazonaws.services.groundstation.AWSGroundStationAsync []
    (AWSGroundStationAsyncClientBuilder/defaultClient )))

