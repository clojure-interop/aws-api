(ns com.amazonaws.services.clouddirectory.AmazonCloudDirectoryAsyncClientBuilder
  "Fluent builder for AmazonCloudDirectoryAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.clouddirectory AmazonCloudDirectoryAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.clouddirectory.AmazonCloudDirectoryAsyncClientBuilder`"
  (^com.amazonaws.services.clouddirectory.AmazonCloudDirectoryAsyncClientBuilder []
    (AmazonCloudDirectoryAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.clouddirectory.AmazonCloudDirectoryAsync`"
  (^com.amazonaws.services.clouddirectory.AmazonCloudDirectoryAsync []
    (AmazonCloudDirectoryAsyncClientBuilder/defaultClient )))

