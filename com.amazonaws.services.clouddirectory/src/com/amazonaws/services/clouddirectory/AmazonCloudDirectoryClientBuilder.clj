(ns com.amazonaws.services.clouddirectory.AmazonCloudDirectoryClientBuilder
  "Fluent builder for AmazonCloudDirectory. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.clouddirectory AmazonCloudDirectoryClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.clouddirectory.AmazonCloudDirectoryClientBuilder`"
  (^com.amazonaws.services.clouddirectory.AmazonCloudDirectoryClientBuilder []
    (AmazonCloudDirectoryClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.clouddirectory.AmazonCloudDirectory`"
  (^com.amazonaws.services.clouddirectory.AmazonCloudDirectory []
    (AmazonCloudDirectoryClientBuilder/defaultClient )))

