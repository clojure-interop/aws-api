(ns com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2AsyncClientBuilder
  "Fluent builder for AWSCloudHSMV2Async. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cloudhsmv2 AWSCloudHSMV2AsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2AsyncClientBuilder`"
  (^com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2AsyncClientBuilder []
    (AWSCloudHSMV2AsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2Async`"
  (^com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2Async []
    (AWSCloudHSMV2AsyncClientBuilder/defaultClient )))

