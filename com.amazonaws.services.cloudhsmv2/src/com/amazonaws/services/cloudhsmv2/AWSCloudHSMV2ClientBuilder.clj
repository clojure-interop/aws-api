(ns com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2ClientBuilder
  "Fluent builder for AWSCloudHSMV2. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cloudhsmv2 AWSCloudHSMV2ClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2ClientBuilder`"
  (^com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2ClientBuilder []
    (AWSCloudHSMV2ClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2`"
  (^com.amazonaws.services.cloudhsmv2.AWSCloudHSMV2 []
    (AWSCloudHSMV2ClientBuilder/defaultClient )))

