(ns com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphAsyncClientBuilder
  "Fluent builder for AWSIoTThingsGraphAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.iotthingsgraph AWSIoTThingsGraphAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphAsyncClientBuilder`"
  (^com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphAsyncClientBuilder []
    (AWSIoTThingsGraphAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphAsync`"
  (^com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphAsync []
    (AWSIoTThingsGraphAsyncClientBuilder/defaultClient )))

