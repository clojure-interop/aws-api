(ns com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphClientBuilder
  "Fluent builder for AWSIoTThingsGraph. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.iotthingsgraph AWSIoTThingsGraphClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphClientBuilder`"
  (^com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraphClientBuilder []
    (AWSIoTThingsGraphClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraph`"
  (^com.amazonaws.services.iotthingsgraph.AWSIoTThingsGraph []
    (AWSIoTThingsGraphClientBuilder/defaultClient )))

