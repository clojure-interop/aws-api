(ns com.amazonaws.services.simpledb.AmazonSimpleDBAsyncClientBuilder
  "Fluent builder for AmazonSimpleDBAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.simpledb AmazonSimpleDBAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.simpledb.AmazonSimpleDBAsyncClientBuilder`"
  (^com.amazonaws.services.simpledb.AmazonSimpleDBAsyncClientBuilder []
    (AmazonSimpleDBAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.simpledb.AmazonSimpleDBAsync`"
  (^com.amazonaws.services.simpledb.AmazonSimpleDBAsync []
    (AmazonSimpleDBAsyncClientBuilder/defaultClient )))

