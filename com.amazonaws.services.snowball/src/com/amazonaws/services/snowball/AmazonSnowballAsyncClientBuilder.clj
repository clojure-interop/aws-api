(ns com.amazonaws.services.snowball.AmazonSnowballAsyncClientBuilder
  "Fluent builder for AmazonSnowballAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.snowball AmazonSnowballAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.snowball.AmazonSnowballAsyncClientBuilder`"
  (^com.amazonaws.services.snowball.AmazonSnowballAsyncClientBuilder []
    (AmazonSnowballAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.snowball.AmazonSnowballAsync`"
  (^com.amazonaws.services.snowball.AmazonSnowballAsync []
    (AmazonSnowballAsyncClientBuilder/defaultClient )))

