(ns com.amazonaws.services.cloudtrail.AWSCloudTrailAsyncClientBuilder
  "Fluent builder for AWSCloudTrailAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cloudtrail AWSCloudTrailAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cloudtrail.AWSCloudTrailAsyncClientBuilder`"
  (^com.amazonaws.services.cloudtrail.AWSCloudTrailAsyncClientBuilder []
    (AWSCloudTrailAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cloudtrail.AWSCloudTrailAsync`"
  (^com.amazonaws.services.cloudtrail.AWSCloudTrailAsync []
    (AWSCloudTrailAsyncClientBuilder/defaultClient )))

