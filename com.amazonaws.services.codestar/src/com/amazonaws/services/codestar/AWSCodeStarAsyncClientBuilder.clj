(ns com.amazonaws.services.codestar.AWSCodeStarAsyncClientBuilder
  "Fluent builder for AWSCodeStarAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.codestar AWSCodeStarAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.codestar.AWSCodeStarAsyncClientBuilder`"
  (^com.amazonaws.services.codestar.AWSCodeStarAsyncClientBuilder []
    (AWSCodeStarAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.codestar.AWSCodeStarAsync`"
  (^com.amazonaws.services.codestar.AWSCodeStarAsync []
    (AWSCodeStarAsyncClientBuilder/defaultClient )))

