(ns com.amazonaws.services.elasticsearch.AWSElasticsearchAsyncClientBuilder
  "Fluent builder for AWSElasticsearchAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.elasticsearch AWSElasticsearchAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.elasticsearch.AWSElasticsearchAsyncClientBuilder`"
  (^com.amazonaws.services.elasticsearch.AWSElasticsearchAsyncClientBuilder []
    (AWSElasticsearchAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.elasticsearch.AWSElasticsearchAsync`"
  (^com.amazonaws.services.elasticsearch.AWSElasticsearchAsync []
    (AWSElasticsearchAsyncClientBuilder/defaultClient )))

