(ns com.amazonaws.services.devicefarm.AWSDeviceFarmAsyncClientBuilder
  "Fluent builder for AWSDeviceFarmAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.devicefarm AWSDeviceFarmAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.devicefarm.AWSDeviceFarmAsyncClientBuilder`"
  (^com.amazonaws.services.devicefarm.AWSDeviceFarmAsyncClientBuilder []
    (AWSDeviceFarmAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.devicefarm.AWSDeviceFarmAsync`"
  (^com.amazonaws.services.devicefarm.AWSDeviceFarmAsync []
    (AWSDeviceFarmAsyncClientBuilder/defaultClient )))

