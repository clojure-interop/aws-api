(ns com.amazonaws.services.certificatemanager.AWSCertificateManagerAsyncClientBuilder
  "Fluent builder for AWSCertificateManagerAsync. Use of the builder
  is preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.certificatemanager AWSCertificateManagerAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.certificatemanager.AWSCertificateManagerAsyncClientBuilder`"
  (^com.amazonaws.services.certificatemanager.AWSCertificateManagerAsyncClientBuilder []
    (AWSCertificateManagerAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.certificatemanager.AWSCertificateManagerAsync`"
  (^com.amazonaws.services.certificatemanager.AWSCertificateManagerAsync []
    (AWSCertificateManagerAsyncClientBuilder/defaultClient )))

