(ns com.amazonaws.services.cloudhsm.AWSCloudHSMAsyncClientBuilder
  "Fluent builder for AWSCloudHSMAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cloudhsm AWSCloudHSMAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cloudhsm.AWSCloudHSMAsyncClientBuilder`"
  (^com.amazonaws.services.cloudhsm.AWSCloudHSMAsyncClientBuilder []
    (AWSCloudHSMAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cloudhsm.AWSCloudHSMAsync`"
  (^com.amazonaws.services.cloudhsm.AWSCloudHSMAsync []
    (AWSCloudHSMAsyncClientBuilder/defaultClient )))

