(ns com.amazonaws.services.dax.AmazonDaxAsyncClientBuilder
  "Fluent builder for AmazonDaxAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.dax AmazonDaxAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.dax.AmazonDaxAsyncClientBuilder`"
  (^com.amazonaws.services.dax.AmazonDaxAsyncClientBuilder []
    (AmazonDaxAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.dax.AmazonDaxAsync`"
  (^com.amazonaws.services.dax.AmazonDaxAsync []
    (AmazonDaxAsyncClientBuilder/defaultClient )))

