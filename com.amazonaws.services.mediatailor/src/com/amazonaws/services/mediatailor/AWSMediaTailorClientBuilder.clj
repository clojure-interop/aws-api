(ns com.amazonaws.services.mediatailor.AWSMediaTailorClientBuilder
  "Fluent builder for AWSMediaTailor. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.mediatailor AWSMediaTailorClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.mediatailor.AWSMediaTailorClientBuilder`"
  (^com.amazonaws.services.mediatailor.AWSMediaTailorClientBuilder []
    (AWSMediaTailorClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.mediatailor.AWSMediaTailor`"
  (^com.amazonaws.services.mediatailor.AWSMediaTailor []
    (AWSMediaTailorClientBuilder/defaultClient )))

