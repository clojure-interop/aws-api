(ns com.amazonaws.services.mediatailor.AWSMediaTailorAsyncClientBuilder
  "Fluent builder for AWSMediaTailorAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.mediatailor AWSMediaTailorAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.mediatailor.AWSMediaTailorAsyncClientBuilder`"
  (^com.amazonaws.services.mediatailor.AWSMediaTailorAsyncClientBuilder []
    (AWSMediaTailorAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.mediatailor.AWSMediaTailorAsync`"
  (^com.amazonaws.services.mediatailor.AWSMediaTailorAsync []
    (AWSMediaTailorAsyncClientBuilder/defaultClient )))

