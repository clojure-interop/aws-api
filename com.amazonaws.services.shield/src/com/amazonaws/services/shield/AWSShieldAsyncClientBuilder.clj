(ns com.amazonaws.services.shield.AWSShieldAsyncClientBuilder
  "Fluent builder for AWSShieldAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.shield AWSShieldAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.shield.AWSShieldAsyncClientBuilder`"
  (^com.amazonaws.services.shield.AWSShieldAsyncClientBuilder []
    (AWSShieldAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.shield.AWSShieldAsync`"
  (^com.amazonaws.services.shield.AWSShieldAsync []
    (AWSShieldAsyncClientBuilder/defaultClient )))

