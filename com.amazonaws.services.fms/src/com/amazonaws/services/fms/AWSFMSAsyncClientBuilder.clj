(ns com.amazonaws.services.fms.AWSFMSAsyncClientBuilder
  "Fluent builder for AWSFMSAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.fms AWSFMSAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.fms.AWSFMSAsyncClientBuilder`"
  (^com.amazonaws.services.fms.AWSFMSAsyncClientBuilder []
    (AWSFMSAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.fms.AWSFMSAsync`"
  (^com.amazonaws.services.fms.AWSFMSAsync []
    (AWSFMSAsyncClientBuilder/defaultClient )))

