(ns com.amazonaws.auth.NoSessionSupportCredentials
  "Marker interface for credentials that should not be used to start
  AWS Security Token Service sessions."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.auth NoSessionSupportCredentials]))

