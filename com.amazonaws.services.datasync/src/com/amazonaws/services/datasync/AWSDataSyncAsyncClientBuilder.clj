(ns com.amazonaws.services.datasync.AWSDataSyncAsyncClientBuilder
  "Fluent builder for AWSDataSyncAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.datasync AWSDataSyncAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.datasync.AWSDataSyncAsyncClientBuilder`"
  (^com.amazonaws.services.datasync.AWSDataSyncAsyncClientBuilder []
    (AWSDataSyncAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.datasync.AWSDataSyncAsync`"
  (^com.amazonaws.services.datasync.AWSDataSyncAsync []
    (AWSDataSyncAsyncClientBuilder/defaultClient )))

