(ns com.amazonaws.services.translate.AmazonTranslateAsyncClientBuilder
  "Fluent builder for AmazonTranslateAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.translate AmazonTranslateAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.translate.AmazonTranslateAsyncClientBuilder`"
  (^com.amazonaws.services.translate.AmazonTranslateAsyncClientBuilder []
    (AmazonTranslateAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.translate.AmazonTranslateAsync`"
  (^com.amazonaws.services.translate.AmazonTranslateAsync []
    (AmazonTranslateAsyncClientBuilder/defaultClient )))

