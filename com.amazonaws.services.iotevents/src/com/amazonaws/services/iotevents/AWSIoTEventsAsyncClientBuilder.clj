(ns com.amazonaws.services.iotevents.AWSIoTEventsAsyncClientBuilder
  "Fluent builder for AWSIoTEventsAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.iotevents AWSIoTEventsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.iotevents.AWSIoTEventsAsyncClientBuilder`"
  (^com.amazonaws.services.iotevents.AWSIoTEventsAsyncClientBuilder []
    (AWSIoTEventsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.iotevents.AWSIoTEventsAsync`"
  (^com.amazonaws.services.iotevents.AWSIoTEventsAsync []
    (AWSIoTEventsAsyncClientBuilder/defaultClient )))

