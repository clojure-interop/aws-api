(ns com.amazonaws.services.iotdata.AWSIotDataAsyncClientBuilder
  "Fluent builder for AWSIotDataAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.iotdata AWSIotDataAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.iotdata.AWSIotDataAsyncClientBuilder`"
  (^com.amazonaws.services.iotdata.AWSIotDataAsyncClientBuilder []
    (AWSIotDataAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.iotdata.AWSIotDataAsync`"
  (^com.amazonaws.services.iotdata.AWSIotDataAsync []
    (AWSIotDataAsyncClientBuilder/defaultClient )))

