(ns com.amazonaws.services.connect.AmazonConnectAsyncClientBuilder
  "Fluent builder for AmazonConnectAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.connect AmazonConnectAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.connect.AmazonConnectAsyncClientBuilder`"
  (^com.amazonaws.services.connect.AmazonConnectAsyncClientBuilder []
    (AmazonConnectAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.connect.AmazonConnectAsync`"
  (^com.amazonaws.services.connect.AmazonConnectAsync []
    (AmazonConnectAsyncClientBuilder/defaultClient )))

