(ns com.amazonaws.services.elasticbeanstalk.AWSElasticBeanstalkAsyncClientBuilder
  "Fluent builder for AWSElasticBeanstalkAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.elasticbeanstalk AWSElasticBeanstalkAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.elasticbeanstalk.AWSElasticBeanstalkAsyncClientBuilder`"
  (^com.amazonaws.services.elasticbeanstalk.AWSElasticBeanstalkAsyncClientBuilder []
    (AWSElasticBeanstalkAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.elasticbeanstalk.AWSElasticBeanstalkAsync`"
  (^com.amazonaws.services.elasticbeanstalk.AWSElasticBeanstalkAsync []
    (AWSElasticBeanstalkAsyncClientBuilder/defaultClient )))

