(ns com.amazonaws.services.licensemanager.AWSLicenseManagerAsyncClientBuilder
  "Fluent builder for AWSLicenseManagerAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.licensemanager AWSLicenseManagerAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.licensemanager.AWSLicenseManagerAsyncClientBuilder`"
  (^com.amazonaws.services.licensemanager.AWSLicenseManagerAsyncClientBuilder []
    (AWSLicenseManagerAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.licensemanager.AWSLicenseManagerAsync`"
  (^com.amazonaws.services.licensemanager.AWSLicenseManagerAsync []
    (AWSLicenseManagerAsyncClientBuilder/defaultClient )))

