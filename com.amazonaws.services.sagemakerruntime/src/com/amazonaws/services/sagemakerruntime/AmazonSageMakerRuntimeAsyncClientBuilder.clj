(ns com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeAsyncClientBuilder
  "Fluent builder for AmazonSageMakerRuntimeAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.sagemakerruntime AmazonSageMakerRuntimeAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeAsyncClientBuilder`"
  (^com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeAsyncClientBuilder []
    (AmazonSageMakerRuntimeAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeAsync`"
  (^com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeAsync []
    (AmazonSageMakerRuntimeAsyncClientBuilder/defaultClient )))

