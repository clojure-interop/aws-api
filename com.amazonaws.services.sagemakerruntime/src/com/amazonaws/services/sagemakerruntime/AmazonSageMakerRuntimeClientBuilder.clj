(ns com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeClientBuilder
  "Fluent builder for AmazonSageMakerRuntime. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.sagemakerruntime AmazonSageMakerRuntimeClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeClientBuilder`"
  (^com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntimeClientBuilder []
    (AmazonSageMakerRuntimeClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntime`"
  (^com.amazonaws.services.sagemakerruntime.AmazonSageMakerRuntime []
    (AmazonSageMakerRuntimeClientBuilder/defaultClient )))

