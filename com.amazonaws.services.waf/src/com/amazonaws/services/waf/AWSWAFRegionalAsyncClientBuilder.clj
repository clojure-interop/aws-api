(ns com.amazonaws.services.waf.AWSWAFRegionalAsyncClientBuilder
  "Fluent builder for AWSWAFRegionalAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.waf AWSWAFRegionalAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.waf.AWSWAFRegionalAsyncClientBuilder`"
  (^com.amazonaws.services.waf.AWSWAFRegionalAsyncClientBuilder []
    (AWSWAFRegionalAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.waf.AWSWAFRegionalAsync`"
  (^com.amazonaws.services.waf.AWSWAFRegionalAsync []
    (AWSWAFRegionalAsyncClientBuilder/defaultClient )))

