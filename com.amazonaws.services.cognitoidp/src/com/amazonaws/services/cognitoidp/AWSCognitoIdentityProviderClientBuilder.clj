(ns com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderClientBuilder
  "Fluent builder for AWSCognitoIdentityProvider. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cognitoidp AWSCognitoIdentityProviderClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderClientBuilder`"
  (^com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderClientBuilder []
    (AWSCognitoIdentityProviderClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cognitoidp.AWSCognitoIdentityProvider`"
  (^com.amazonaws.services.cognitoidp.AWSCognitoIdentityProvider []
    (AWSCognitoIdentityProviderClientBuilder/defaultClient )))

