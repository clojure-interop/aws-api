(ns com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderAsyncClientBuilder
  "Fluent builder for AWSCognitoIdentityProviderAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cognitoidp AWSCognitoIdentityProviderAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderAsyncClientBuilder`"
  (^com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderAsyncClientBuilder []
    (AWSCognitoIdentityProviderAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderAsync`"
  (^com.amazonaws.services.cognitoidp.AWSCognitoIdentityProviderAsync []
    (AWSCognitoIdentityProviderAsyncClientBuilder/defaultClient )))

