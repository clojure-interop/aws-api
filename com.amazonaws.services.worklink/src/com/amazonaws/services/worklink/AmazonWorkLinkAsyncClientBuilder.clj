(ns com.amazonaws.services.worklink.AmazonWorkLinkAsyncClientBuilder
  "Fluent builder for AmazonWorkLinkAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.worklink AmazonWorkLinkAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.worklink.AmazonWorkLinkAsyncClientBuilder`"
  (^com.amazonaws.services.worklink.AmazonWorkLinkAsyncClientBuilder []
    (AmazonWorkLinkAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.worklink.AmazonWorkLinkAsync`"
  (^com.amazonaws.services.worklink.AmazonWorkLinkAsync []
    (AmazonWorkLinkAsyncClientBuilder/defaultClient )))

