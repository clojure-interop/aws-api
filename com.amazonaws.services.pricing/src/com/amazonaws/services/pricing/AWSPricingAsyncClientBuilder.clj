(ns com.amazonaws.services.pricing.AWSPricingAsyncClientBuilder
  "Fluent builder for AWSPricingAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.pricing AWSPricingAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.pricing.AWSPricingAsyncClientBuilder`"
  (^com.amazonaws.services.pricing.AWSPricingAsyncClientBuilder []
    (AWSPricingAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.pricing.AWSPricingAsync`"
  (^com.amazonaws.services.pricing.AWSPricingAsync []
    (AWSPricingAsyncClientBuilder/defaultClient )))

