(ns com.amazonaws.services.opsworkscm.AWSOpsWorksCMClientBuilder
  "Fluent builder for AWSOpsWorksCM. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.opsworkscm AWSOpsWorksCMClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.opsworkscm.AWSOpsWorksCMClientBuilder`"
  (^com.amazonaws.services.opsworkscm.AWSOpsWorksCMClientBuilder []
    (AWSOpsWorksCMClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.opsworkscm.AWSOpsWorksCM`"
  (^com.amazonaws.services.opsworkscm.AWSOpsWorksCM []
    (AWSOpsWorksCMClientBuilder/defaultClient )))

