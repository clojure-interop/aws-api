(ns com.amazonaws.services.opsworkscm.AWSOpsWorksCMAsyncClientBuilder
  "Fluent builder for AWSOpsWorksCMAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.opsworkscm AWSOpsWorksCMAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.opsworkscm.AWSOpsWorksCMAsyncClientBuilder`"
  (^com.amazonaws.services.opsworkscm.AWSOpsWorksCMAsyncClientBuilder []
    (AWSOpsWorksCMAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.opsworkscm.AWSOpsWorksCMAsync`"
  (^com.amazonaws.services.opsworkscm.AWSOpsWorksCMAsync []
    (AWSOpsWorksCMAsyncClientBuilder/defaultClient )))

