(ns com.amazonaws.services.directory.AWSDirectoryServiceAsyncClientBuilder
  "Fluent builder for AWSDirectoryServiceAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.directory AWSDirectoryServiceAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.directory.AWSDirectoryServiceAsyncClientBuilder`"
  (^com.amazonaws.services.directory.AWSDirectoryServiceAsyncClientBuilder []
    (AWSDirectoryServiceAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.directory.AWSDirectoryServiceAsync`"
  (^com.amazonaws.services.directory.AWSDirectoryServiceAsync []
    (AWSDirectoryServiceAsyncClientBuilder/defaultClient )))

