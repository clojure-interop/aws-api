(ns com.amazonaws.services.dlm.AmazonDLMAsyncClientBuilder
  "Fluent builder for AmazonDLMAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.dlm AmazonDLMAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.dlm.AmazonDLMAsyncClientBuilder`"
  (^com.amazonaws.services.dlm.AmazonDLMAsyncClientBuilder []
    (AmazonDLMAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.dlm.AmazonDLMAsync`"
  (^com.amazonaws.services.dlm.AmazonDLMAsync []
    (AmazonDLMAsyncClientBuilder/defaultClient )))

