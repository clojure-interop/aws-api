(ns com.amazonaws.services.importexport.AmazonImportExportClientBuilder
  "Fluent builder for AmazonImportExport. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.importexport AmazonImportExportClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.importexport.AmazonImportExportClientBuilder`"
  (^com.amazonaws.services.importexport.AmazonImportExportClientBuilder []
    (AmazonImportExportClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.importexport.AmazonImportExport`"
  (^com.amazonaws.services.importexport.AmazonImportExport []
    (AmazonImportExportClientBuilder/defaultClient )))

