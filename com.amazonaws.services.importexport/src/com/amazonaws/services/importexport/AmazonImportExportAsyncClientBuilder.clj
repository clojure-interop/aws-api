(ns com.amazonaws.services.importexport.AmazonImportExportAsyncClientBuilder
  "Fluent builder for AmazonImportExportAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.importexport AmazonImportExportAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.importexport.AmazonImportExportAsyncClientBuilder`"
  (^com.amazonaws.services.importexport.AmazonImportExportAsyncClientBuilder []
    (AmazonImportExportAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.importexport.AmazonImportExportAsync`"
  (^com.amazonaws.services.importexport.AmazonImportExportAsync []
    (AmazonImportExportAsyncClientBuilder/defaultClient )))

