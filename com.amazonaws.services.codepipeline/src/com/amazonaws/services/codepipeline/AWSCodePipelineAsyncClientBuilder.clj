(ns com.amazonaws.services.codepipeline.AWSCodePipelineAsyncClientBuilder
  "Fluent builder for AWSCodePipelineAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.codepipeline AWSCodePipelineAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.codepipeline.AWSCodePipelineAsyncClientBuilder`"
  (^com.amazonaws.services.codepipeline.AWSCodePipelineAsyncClientBuilder []
    (AWSCodePipelineAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.codepipeline.AWSCodePipelineAsync`"
  (^com.amazonaws.services.codepipeline.AWSCodePipelineAsync []
    (AWSCodePipelineAsyncClientBuilder/defaultClient )))

