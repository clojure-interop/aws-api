(ns com.amazonaws.services.medialive.AWSMediaLiveAsyncClientBuilder
  "Fluent builder for AWSMediaLiveAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.medialive AWSMediaLiveAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.medialive.AWSMediaLiveAsyncClientBuilder`"
  (^com.amazonaws.services.medialive.AWSMediaLiveAsyncClientBuilder []
    (AWSMediaLiveAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.medialive.AWSMediaLiveAsync`"
  (^com.amazonaws.services.medialive.AWSMediaLiveAsync []
    (AWSMediaLiveAsyncClientBuilder/defaultClient )))

