(ns com.amazonaws.services.pinpointemail.AmazonPinpointEmailClientBuilder
  "Fluent builder for AmazonPinpointEmail. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.pinpointemail AmazonPinpointEmailClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.pinpointemail.AmazonPinpointEmailClientBuilder`"
  (^com.amazonaws.services.pinpointemail.AmazonPinpointEmailClientBuilder []
    (AmazonPinpointEmailClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.pinpointemail.AmazonPinpointEmail`"
  (^com.amazonaws.services.pinpointemail.AmazonPinpointEmail []
    (AmazonPinpointEmailClientBuilder/defaultClient )))

