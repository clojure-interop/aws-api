(ns com.amazonaws.endpointdiscovery.DefaultEndpointDiscoveryProviderChain
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.endpointdiscovery DefaultEndpointDiscoveryProviderChain]))

(defn ->default-endpoint-discovery-provider-chain
  "Constructor."
  (^DefaultEndpointDiscoveryProviderChain []
    (new DefaultEndpointDiscoveryProviderChain )))

