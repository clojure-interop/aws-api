(ns com.amazonaws.services.simplesystemsmanagement.AWSSimpleSystemsManagementAsyncClientBuilder
  "Fluent builder for AWSSimpleSystemsManagementAsync. Use of the
  builder is preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.simplesystemsmanagement AWSSimpleSystemsManagementAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.simplesystemsmanagement.AWSSimpleSystemsManagementAsyncClientBuilder`"
  (^com.amazonaws.services.simplesystemsmanagement.AWSSimpleSystemsManagementAsyncClientBuilder []
    (AWSSimpleSystemsManagementAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.simplesystemsmanagement.AWSSimpleSystemsManagementAsync`"
  (^com.amazonaws.services.simplesystemsmanagement.AWSSimpleSystemsManagementAsync []
    (AWSSimpleSystemsManagementAsyncClientBuilder/defaultClient )))

