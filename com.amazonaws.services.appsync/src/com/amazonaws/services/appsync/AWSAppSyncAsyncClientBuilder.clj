(ns com.amazonaws.services.appsync.AWSAppSyncAsyncClientBuilder
  "Fluent builder for AWSAppSyncAsync. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.appsync AWSAppSyncAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.appsync.AWSAppSyncAsyncClientBuilder`"
  (^com.amazonaws.services.appsync.AWSAppSyncAsyncClientBuilder []
    (AWSAppSyncAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.appsync.AWSAppSyncAsync`"
  (^com.amazonaws.services.appsync.AWSAppSyncAsync []
    (AWSAppSyncAsyncClientBuilder/defaultClient )))

