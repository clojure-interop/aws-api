(ns com.amazonaws.services.cognitoidentity.AmazonCognitoIdentityClientBuilder
  "Fluent builder for AmazonCognitoIdentity. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.cognitoidentity AmazonCognitoIdentityClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.cognitoidentity.AmazonCognitoIdentityClientBuilder`"
  (^com.amazonaws.services.cognitoidentity.AmazonCognitoIdentityClientBuilder []
    (AmazonCognitoIdentityClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.cognitoidentity.AmazonCognitoIdentity`"
  (^com.amazonaws.services.cognitoidentity.AmazonCognitoIdentity []
    (AmazonCognitoIdentityClientBuilder/defaultClient )))

