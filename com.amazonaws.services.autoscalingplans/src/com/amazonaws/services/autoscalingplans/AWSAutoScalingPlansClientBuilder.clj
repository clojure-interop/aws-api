(ns com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansClientBuilder
  "Fluent builder for AWSAutoScalingPlans. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.autoscalingplans AWSAutoScalingPlansClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansClientBuilder`"
  (^com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansClientBuilder []
    (AWSAutoScalingPlansClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.autoscalingplans.AWSAutoScalingPlans`"
  (^com.amazonaws.services.autoscalingplans.AWSAutoScalingPlans []
    (AWSAutoScalingPlansClientBuilder/defaultClient )))

