(ns com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansAsyncClientBuilder
  "Fluent builder for AWSAutoScalingPlansAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.autoscalingplans AWSAutoScalingPlansAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansAsyncClientBuilder`"
  (^com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansAsyncClientBuilder []
    (AWSAutoScalingPlansAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansAsync`"
  (^com.amazonaws.services.autoscalingplans.AWSAutoScalingPlansAsync []
    (AWSAutoScalingPlansAsyncClientBuilder/defaultClient )))

