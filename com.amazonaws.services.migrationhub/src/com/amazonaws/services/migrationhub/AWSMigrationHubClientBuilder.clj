(ns com.amazonaws.services.migrationhub.AWSMigrationHubClientBuilder
  "Fluent builder for AWSMigrationHub. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.migrationhub AWSMigrationHubClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.migrationhub.AWSMigrationHubClientBuilder`"
  (^com.amazonaws.services.migrationhub.AWSMigrationHubClientBuilder []
    (AWSMigrationHubClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.migrationhub.AWSMigrationHub`"
  (^com.amazonaws.services.migrationhub.AWSMigrationHub []
    (AWSMigrationHubClientBuilder/defaultClient )))

