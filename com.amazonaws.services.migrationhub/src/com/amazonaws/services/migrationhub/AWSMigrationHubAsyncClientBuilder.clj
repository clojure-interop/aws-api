(ns com.amazonaws.services.migrationhub.AWSMigrationHubAsyncClientBuilder
  "Fluent builder for AWSMigrationHubAsync. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.migrationhub AWSMigrationHubAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.migrationhub.AWSMigrationHubAsyncClientBuilder`"
  (^com.amazonaws.services.migrationhub.AWSMigrationHubAsyncClientBuilder []
    (AWSMigrationHubAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.migrationhub.AWSMigrationHubAsync`"
  (^com.amazonaws.services.migrationhub.AWSMigrationHubAsync []
    (AWSMigrationHubAsyncClientBuilder/defaultClient )))

