(ns com.amazonaws.services.directconnect.AmazonDirectConnectAsyncClientBuilder
  "Fluent builder for AmazonDirectConnectAsync. Use of the builder is
  preferred over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.directconnect AmazonDirectConnectAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.directconnect.AmazonDirectConnectAsyncClientBuilder`"
  (^com.amazonaws.services.directconnect.AmazonDirectConnectAsyncClientBuilder []
    (AmazonDirectConnectAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.directconnect.AmazonDirectConnectAsync`"
  (^com.amazonaws.services.directconnect.AmazonDirectConnectAsync []
    (AmazonDirectConnectAsyncClientBuilder/defaultClient )))

