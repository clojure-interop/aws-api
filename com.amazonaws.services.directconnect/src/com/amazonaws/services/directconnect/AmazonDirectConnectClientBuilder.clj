(ns com.amazonaws.services.directconnect.AmazonDirectConnectClientBuilder
  "Fluent builder for AmazonDirectConnect. Use of the builder is preferred
  over using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.directconnect AmazonDirectConnectClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.directconnect.AmazonDirectConnectClientBuilder`"
  (^com.amazonaws.services.directconnect.AmazonDirectConnectClientBuilder []
    (AmazonDirectConnectClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.directconnect.AmazonDirectConnect`"
  (^com.amazonaws.services.directconnect.AmazonDirectConnect []
    (AmazonDirectConnectClientBuilder/defaultClient )))

