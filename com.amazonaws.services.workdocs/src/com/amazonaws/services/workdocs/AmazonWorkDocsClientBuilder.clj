(ns com.amazonaws.services.workdocs.AmazonWorkDocsClientBuilder
  "Fluent builder for AmazonWorkDocs. Use of the builder is preferred over using
  constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.workdocs AmazonWorkDocsClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.workdocs.AmazonWorkDocsClientBuilder`"
  (^com.amazonaws.services.workdocs.AmazonWorkDocsClientBuilder []
    (AmazonWorkDocsClientBuilder/standard )))

(defn *default-client
  "returns: Default client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.workdocs.AmazonWorkDocs`"
  (^com.amazonaws.services.workdocs.AmazonWorkDocs []
    (AmazonWorkDocsClientBuilder/defaultClient )))

