(ns com.amazonaws.services.workdocs.AmazonWorkDocsAsyncClientBuilder
  "Fluent builder for AmazonWorkDocsAsync. Use of the builder is preferred over
  using constructors of the client class."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.amazonaws.services.workdocs AmazonWorkDocsAsyncClientBuilder]))

(defn *standard
  "returns: Create new instance of builder with all defaults set. - `com.amazonaws.services.workdocs.AmazonWorkDocsAsyncClientBuilder`"
  (^com.amazonaws.services.workdocs.AmazonWorkDocsAsyncClientBuilder []
    (AmazonWorkDocsAsyncClientBuilder/standard )))

(defn *default-client
  "returns: Default async client using the DefaultAWSCredentialsProviderChain and
           DefaultAwsRegionProviderChain chain - `com.amazonaws.services.workdocs.AmazonWorkDocsAsync`"
  (^com.amazonaws.services.workdocs.AmazonWorkDocsAsync []
    (AmazonWorkDocsAsyncClientBuilder/defaultClient )))

